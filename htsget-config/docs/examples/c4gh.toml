# An example of running htsget-rs with Crypt4GH enabled.
# Run with `cargo run -p htsget-axum --all-features -- --config htsget-config/docs/examples/c4gh.toml`

ticket_server.addr = "127.0.0.1:8080"
data_server.addr = "127.0.0.1:8081"

[[locations]]
regex = ".*"
substitution_string = "$0"
backend.kind = "File"

backend.keys.private.kind = "File"
backend.keys.private.key = "data/c4gh/keys/bob.sec" # pragma: allowlist secret
backend.keys.public.kind = "File"
backend.keys.public.key = "data/c4gh/keys/alice.pub"

## Or, use AWS secrets manager to store keys.
#backend.keys.private.kind = "SecretsManager"
#backend.keys.private.key = "htsget/test_c4gh_private_key" # pragma: allowlist secret
#backend.keys.public.kind = "SecretsManager"
#backend.keys.public.key = "htsget/test_c4gh_public_key"
